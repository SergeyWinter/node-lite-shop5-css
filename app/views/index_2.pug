include head

//header(class='fixed-top')
header
  //include site_header
  include site_header3.pug
section(class='sectionWone')
 img(src='images/cigarsFont.jpg')
main(class="text-white bg-dark")
  div(class='row justify-content-center')
    each val2, index2 in catR
      div(class="col-sm-12 col-md-12 col-lg-12")
        h2
          a(
              href='/cat?id='+val2['id']
              ) #{val2['category']}
                
      each val, index in goods
        if val2['id'] == val['category']
          include cards.pug
  include navTo.pug 
  include navToRight.pug                      
  include footer_2.pug       
    
